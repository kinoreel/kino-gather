#!groovy

node {
   stage ('Starting kino-inserts') {
       def kino_tables = ['movies',
                          'at',
                          'me']
       kino_tables.each {
            echo "${it}"
        }
   }
}